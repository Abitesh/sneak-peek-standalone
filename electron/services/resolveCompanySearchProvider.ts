// electron/services/resolveCompanySearchProvider.ts

// Company research search is disabled for the moment.
// This removes the dependency on Natively's premium search
// providers while we build the standalone application.

export function resolveCompanySearchProvider(): null {
  return null;
}